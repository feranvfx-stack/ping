export default function IncomingCallModal({ caller, callType, onAccept, onDecline }) { return <div className="call-overlay"><div className="incoming-call"><div className="call-avatar"><img src={caller?.avatar_url || '/favicon.svg'} alt="" /></div><p className="eyebrow">Incoming {callType === 'video' ? 'video' : 'voice'} call</p><h2>{caller?.display_name || caller?.email || 'Someone'}</h2><div className="call-actions"><button className="decline-call" onClick={onDecline}>Decline</button><button className="accept-call" onClick={onAccept}>Accept</button></div></div></div> }