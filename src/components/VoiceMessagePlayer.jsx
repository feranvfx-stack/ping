import { useRef, useState } from 'react'
export default function VoiceMessagePlayer({ src, duration, mine }) { const audio = useRef(null); const [playing, setPlaying] = useState(false); return <div className="voice-player"><button className="voice-play" onClick={() => { if (playing) audio.current.pause(); else audio.current.play(); setPlaying(!playing) }}>{playing ? 'Ⅱ' : '▶'}</button><div className={`waveform ${mine ? 'mine' : ''}`}>{[3, 6, 10, 7, 14, 8, 5, 12, 9, 15, 7, 4, 11, 6].map((height, index) => <i key={index} style={{ height: `${height * 2}px` }} />)}</div><span>{Math.round(duration || 0)}s</span><audio ref={audio} src={src} onEnded={() => setPlaying(false)} /></div> }